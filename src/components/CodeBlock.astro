---
interface Props {
  lang: string;
  caption?: string;
}

const { lang, caption } = Astro.props;
---
<div class="code">
  <div class="code-hdr">
    <span>{lang}</span>
    {caption && <span>{caption}</span>}
  </div>
  <div class="code-body"><slot /></div>
</div>
