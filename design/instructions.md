**Read `Prototype v4.html` in full.** Read it top to bottom — don't skim.

**If anything is ambiguous, ask the user to confirm before you start implementing.** It's much cheaper to clarify scope up front than to build the wrong thing.

## About the design files

The design medium is **HTML/CSS/JS** — these are prototypes, not production code. Your job is to **recreate them pixel-perfectly** in whatever technology makes sense using Astro framework (astro.build using bun and astro CLI). Match the visual output; don't copy the prototype's internal structure unless it happens to fit.

Everything you need — dimensions, colors, layout rules — is spelled out in the source. Read the HTML and CSS directly; No need for screenshots.

## Bundle contents

- `./instructions.md` — this file
- `./justanotherspy-identity-v05.html` - a visual identity document that is a work in progress (only used as an input for building the prototype. The prototype is the newer approved visual identity)
- `./Prototype v4.html` — the project prototype file (HTML prototypes)
